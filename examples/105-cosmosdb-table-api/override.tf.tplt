terraform {
  required_providers {
    azurerm = {
      source  = "hashicorp/azurerm"
      version = "{{ env "TERRAFORM_AZURERM_VERSION" | default ">=3.0"}}"
    }
  }
}

module "azure_cosmos_db" {
  source = "{{env "MODULE_SOURCE" |  default "../../"}}"
}